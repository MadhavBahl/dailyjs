/**
 * Take an array of numbers and convert each element into a string
 */

const numbers = [1, 10, 20, 30, 40, 50];

let convertedNumbers = numbers.map (number => number.toString());

console.log (convertedNumbers);