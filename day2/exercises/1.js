/**
 * Ques) Make an array of numbers that are doubles of the first array  
 */

const numbers = [1, 2, 3, 4, 5];

let doubledNumbers = numbers.map (number => 2*number);

console.log (doubledNumbers);